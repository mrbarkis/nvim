-- Adds file spesicfig commenting:
--     gcc -> toggle current line
--     gc -> toggle current selection
--     gbc -> toggle current block
--     gb -> toggle current selection

return {
	"numToStr/Comment.nvim",
	opts = {},
}
